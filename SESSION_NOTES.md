# Session Notes (2026-04-28)

## Что сделали
- Выяснили причину, почему «история» не загружалась: она была только в local state и не хранилась в БД.
- Добавили серверное хранение истории сообщений по задачам:
  - таблица `task_messages` в SQLite
  - API: `GET/POST /api/tasks/[id]/messages`
  - подключили загрузку и отправку сообщений в `components/TaskBoard.tsx`
- Подняли проект в Docker и проверили, что контейнер работает.

## Текущий статус окружения
- Приложение запущено в контейнере `bug-tracker-app`
- Адрес: http://localhost:3000
- БД в контейнере: `/data/bugtracker.db`

## Важно про диалог
- История этого чата хранится в интерфейсе/сессии клиента, а не в репозитории.
- Для надежности ключевые итоги фиксируем в файлах проекта (этот файл).

## Следующие шаги (опционально)
1. Добавить авто-лог изменений в `docs/CHANGELOG_SESSION.md` после каждой важной правки.
2. Сделать git commit с текущими правками, чтобы состояние было восстановимо в любой момент.
3. Добавить экспорт/импорт истории сообщений задач (если нужно переносить между БД).

## Проверка стабильности (добавлено 2026-04-28)
- Локальный `npm run build` в текущей среде падает с `spawn EPERM` (ограничение окружения/прав), это не показатель проблемы кода.
- Проверка через Docker:
  - контейнер `bug-tracker-app` в статусе `Up`
  - `next start` запускается (`Ready`)
  - API отвечает (в логах есть `API: Current user fetched`)
- Кодировка исправлена:
  - восстановлен `components/Sidebar.tsx`
  - восстановлены поврежденные demo-записи в `/data/bugtracker.db`.
